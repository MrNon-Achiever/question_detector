# MewCode 对话助手 Spec

## 背景

用户想从零构建一个命令行 AI 编程助手 MewCode（类似 Claude Code），用 Python 实现。这是分阶段构建的第一个里程碑：先做"纯对话"层——终端交互界面、流式回复、多轮记忆、双后端（Anthropic Claude / OpenAI）。后续阶段才会加入 tool use、文件操作、代码编辑等 agent 能力，本阶段不涉及。

现有一份不完整的参考源码（`mewcode-python/`），仅作方向参考，不直接复用、不逐行对照。

## 目标

- 提供可启动的终端交互式对话界面（TUI）
- 用户输入问题后，调用配置指定的大模型 API，流式逐字打印回复
- 支持多轮对话，AI 在同一会话内记住上下文
- 通过 YAML 配置切换 Anthropic / OpenAI 两种后端，Provider 抽象为统一接口，便于未来扩展
- 支持 Claude 的 extended thinking，思考内容可视化展示

## 功能需求

### F1: 配置管理

- 从 YAML 配置文件读取 LLM 供应商列表，每条配置含 6 个字段：
  - `name`：供应商标识名，用于区分多个配置
  - `protocol`：决定走哪家协议（anthropic / openai）
  - `model`：指定模型
  - `base_url`：请求的地址（允许自定义/代理/本地端点）
  - `api_key`：认证凭据
  - `thinking`：是否启用扩展思考（可选，默认不启用）
- 支持多个供应商配置并存，配置中用一个 `default` 字段指定当前激活的供应商 name
- 启动时支持 `--provider <name>` 命令行参数覆盖 default 选择；未指定则用 default
- 配置文件路径默认为当前工作目录下的 `config.yaml`，支持 `--config <path>` 覆盖
- 配置文件缺失、格式非法、必填字段缺失、或 name/default 不匹配时，给出清晰错误提示并退出

### F2: Provider 抽象层

- 定义统一的 Provider 接口，屏蔽 Anthropic / OpenAI 协议差异
- 根据 `protocol` 字段选择对应实现（anthropic / openai）
- 对外暴露统一的"发送对话历史 + 系统提示词，返回流式回复"能力，不向上层暴露各家协议细节
- 新增后端只需新增一个 Provider 实现，不改动 TUI 与对话管理

### F3: 流式回复

- 使用 SSE 流式接收回复，逐 token 打印，不等全部生成完再返回
- 流式输出时渲染 Markdown（代码块、标题、列表等）
- 流式过程中可被用户中断（Ctrl+C），中断后回到输入提示符，已生成内容保留

### F4: 多轮对话记忆

- 维护当前会话的完整对话历史（用户消息 + 助手回复）
- 每次请求将完整历史一并发送给 API
- `/clear` 命令清空历史，重新开始

### F5: Extended Thinking

- 当某供应商配置 `thinking` 为 true 时，向该 API 请求扩展思考
- 思考内容以区别样式（暗淡/斜体）流式显示在最终回答之前
- 当 `thinking` 为 false 或未设置时，不请求思考，正常返回回复
- `thinking` 字段仅对 anthropic 协议有效；若在 openai 协议下设为 true，启动时给出提示并按不启用处理（不静默忽略）

### F6: 交互界面（TUI）

- 启动后进入交互式对话循环：显示输入提示符，等待用户输入
- 输入支持 ↑↓ 调出历史输入、支持多行输入
- 启动时向 API 发送硬编码系统提示词（定义 MewCode 身份）
- 助手回复以可视化方式（Markdown 渲染）展示

### F7: 会话内命令

- `/exit`：退出程序
- `/clear`：清空对话历史
- `/switch <name>`：切换到指定 name 的供应商配置；name 不存在时报错
- 以 `/` 开头的输入视为命令，与普通对话输入区分；未知命令给出提示

## 非功能需求

- N1: 用 Python 3.12 实现，依赖明确且最小化
- N2: 流式打印低延迟，token 到达即输出，不批量堆积
- N3: api_key 等敏感信息只从配置文件读取，不打印到输出/日志
- N4: 单次请求失败（网络/认证/限流/超时）时，向用户显示错误信息并回到输入提示符，不崩溃
- N5: Provider 抽象使新增后端的改动局部化（不修改 TUI / 对话管理）
- N6: 启动到首次可输入的延迟可接受（不在启动阶段做重活）

## 不做的事

- 不做 tool use / 函数调用
- 不做文件读写、代码编辑、shell 执行等 agent 能力
- 不做跨会话的对话持久化（关闭即丢失，不存盘）
- 不做对话历史的 token 截断/压缩（本阶段无上限，整段发送）
- 不做多会话管理 / 会话恢复
- 不做 API 调用计费统计、token 计数展示
- 不做 Web 界面，只有终端

## 验收标准

- AC1: 在 `config.yaml` 配置一个 anthropic 供应商并设为 default，启动 MewCode，输入问题，能看到回复逐字流式打印且 Markdown 正确渲染（验证 F1 / F3 / F6）
- AC2: 在 `config.yaml` 同时配置 anthropic 和 openai 两个供应商，用 `--provider <openai-name>` 启动，确认走的是 OpenAI 后端且能正常对话（验证 F1 / F2）
- AC3: 同一会话内连续多轮提问，AI 回答能引用前文内容，证明记忆生效；输入 `/clear` 后再提问，AI 不再记得之前内容（验证 F4）
- AC4: 给某 anthropic 供应商设 `thinking: true`，提问后能看到暗淡样式的思考内容出现在最终回答之前（验证 F5）
- AC5: 输入 `/exit` 正常退出；`/clear` 清空历史；`/switch <name>` 切换供应商且后续对话走新供应商；`/switch` 不存在的 name 报错（验证 F7）
- AC6: 流式输出过程中按 Ctrl+C 能中断当前回复，回到输入提示符，程序不崩溃（验证 F3 / N4）
- AC7: 故意填错 api_key 或断网，请求失败时显示错误信息并回到提示符，不崩溃（验证 N4）
- AC8: 删除 `config.yaml` 或字段非法时，启动报清晰错误并退出，不抛堆栈（验证 F1）
- AC9: 代码结构上新增一个 Provider 实现即可接入新后端，无需改动 TUI 与对话管理代码（验证 F2 / N5，通过代码结构审查）
